pyserial-asyncio
pyghmi
spidev
pyrad
types-PyYAML
types-aiofiles
luma.oled
# Dropped from the Arch repos, so it can no longer come from pacman, but it is
# a real dependency: kvmd/plugins/ugpio/pwm.py does `from periphery import PWM`
# at module scope, and the linters in tox.ini all scan `kvmd` wholesale.
python-periphery
# ustreamer's python half runs `python3 -m installer ... dist/*.whl` in its own
# Makefile, and the container has python-build/python-wheel/python-setuptools
# but nothing providing `installer`. The C half builds fine; this is the step
# after it. Taken from pip rather than pacman because the PyPI name is
# unambiguous and this file is already proven to install before the ustreamer
# layer -- there is no route to the Arch mirrors from where this was written,
# so an Arch package name would be a guess.
installer
# kvmd/keyboard/mappings.py does `from evdev import ecodes` at module scope,
# and mappings.py is reached by most of the suite, so its absence is not one
# failing test -- it is 9 collection errors and pytest exiting 2 before running
# anything. It appears in no package list anywhere: not this file, not the
# Dockerfile, not PKGBUILD. A documentation pass flagged it as unsettled and
# this is CI confirming it.
evdev
